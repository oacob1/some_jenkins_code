pipeline {
  agent any
  stages {
    stage('echo') {
      steps {
        echo 'hello from the trigger'
        echo 'another test'
        echo 'configure jenkins server to my public IP so it can be resovled'
      }
    }

  }
}
